<%# content heading %>

<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>

<%- messages() %>

<% if (errors) { %>
    <ul class="notice">
   <% errors.array().forEach(error => { %>
     <li><%= error.msg %></li>
  <%  }) %>
   </ul>
  <% } %>

<form action="/account/register" method="post" id="account_form">
    <input type="text" id="firstName" name="account_firstname" placeholder="First Name" required value="<%= locals.account_firstname %>"><br><br>
    <input type="text" id="lastName" name="account_lastname" placeholder="Last Name" required value="<%= locals.account_lastname %>">><br><br>
    <input type="email" id="email" name="account_email" placeholder="email@example.com" required value="<%= locals.account_email %>">><br><br>
    <input type="password" id="password" name="account_password" placeholder="password" required pattern="^(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[^a-zA-Z0-9])(?!.*\s).{12,}$" title="Please include at least one upper and lower case letter, one number, and at least 12 characters."><br><br>
    <input type="submit" value="register">
    <h2>have an account? <a href="/account/login" class="account_link">log in!</a></h2>
  </form>